import { createAction, props } from '@ngrx/store';

export const handleLoginAction = createAction('[Login Component] Login');
export const handleRoleAction = createAction('[CustomerPage Component] CustomerPage');

